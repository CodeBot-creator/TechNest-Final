<resources>
    <!-- Base app theme -->
    <style name="Base.Theme.TechNestFinal" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- Main app theme -->
    <style name="Theme.TechNestFinal" parent="Base.Theme.TechNestFinal" />

    <!-- Default splash theme: this only sets windowBackground to solid blue -->
    <style name="Theme.TechNestFinal.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>



    </style>
</resources>
